-- Data repair migration intentionally left as a no-op for the public template.
-- Production instances that need source attribution repair should run an
-- instance-specific script outside the public repository.
SELECT 1;
